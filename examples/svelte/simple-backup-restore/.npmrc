engine-strict=true
# This example pins orbitdb-storacha-bridge 0.5.3 from npm, whose peer range for
# lucide-svelte (^0.400.0 || ^0.500.0) predates the 0.539 used here; a local
# file: link never checked it. See the bridge's OrbitDB 4 upgrade PR.
legacy-peer-deps=true
